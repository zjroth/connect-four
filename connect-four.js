// Initialize the page.
function initPage() {
    document.getElementById('divBoard').innerHTML = buildBoard(6, 7);
}

// Build the board.
function buildBoard(numRows, numCols) {
    var htmlTable = '';
    return htmlTable;
}
